# Copy to .env.local for local dev. Set in Vercel → Project → Environment Variables for production.

# Vercel Cron — Authorization: Bearer <CRON_SECRET> on GET /api/cron (random string, ≥16 chars)
CRON_SECRET=

# ISR / on-demand revalidation and IndexNow API — query ?secret= on POST /api/revalidate and /api/indexnow
# See docs/ISR-REVALIDATION.md and docs/INDEXNOW.md (use a long random string; rotate with CRON_SECRET in Vercel)
REVALIDATION_SECRET=

# Google Search Console — HTML tag verification (public, safe to expose)
# https://search.google.com/search-console
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=

# Google Business Profile — public Maps URL (sameAs in JSON-LD; optional hasMap/listing override)
# Use the link from Google Business Profile → Share (maps.google.com, maps.app.goo.gl, etc.).
# When set to a Maps URL, hasMap + listing links may use this URL (see src/config/site-google.ts).
# Example: https://www.google.com/maps?cid=YOUR_CID or https://maps.app.goo.gl/...
NEXT_PUBLIC_GOOGLE_BUSINESS_PROFILE_URL=

# Leave a review — GBP “Ask for reviews” link (defaults in code; override if Google issues a new URL)
# NEXT_PUBLIC_GOOGLE_BUSINESS_REVIEW_URL=

# Optional overrides if you need different URLs than the GBP link (leave unset to use logic above + address fallback).
# NEXT_PUBLIC_GOOGLE_MAPS_LISTING_URL=
# NEXT_PUBLIC_GOOGLE_MAPS_DIRECTIONS_URL=
# NEXT_PUBLIC_GOOGLE_MAPS_EMBED_SRC=

# Cloudflare Images (hosted). Hash is public (in every delivery URL). Token is secret — never commit it.
# Account ID: 2cc579c1ec9e426ed585e933ebf4753b
# Delivery: https://imagedelivery.net/byE6BTe9lNqo21V57n4aPQ/<image_id>/<variant_name>
# NEXT_PUBLIC_CLOUDFLARE_IMAGES_ACCOUNT_HASH=byE6BTe9lNqo21V57n4aPQ
# NEXT_PUBLIC_CLOUDFLARE_IMAGES_HOSTED=true
# CLOUDFLARE_ACCOUNT_ID=2cc579c1ec9e426ed585e933ebf4753b
# CLOUDFLARE_IMAGES_API_TOKEN=
